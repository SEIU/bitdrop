[project]
name = "BitDrop"
version = "2.0.0"
description = "Secure and anonymous file sharing"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "fastapi[standard]",
    "gunicorn",
    "ipython",
    "pydantic",
    "pyyaml",
    "requests",
    "uvicorn",
]
[dependency-groups]
dev = [
    "pre-commit>=4.0.1",
    "pytest >=8.3.3",
    "pytest-dependency>=0.6.0",
    "pytest-xdist>=3.8.0",
    "ruff",
]
[tool.ruff]
# Allow lines to 88 chars (Black compatible).
line-length = 88
